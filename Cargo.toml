[package]
name = "mcc"
version = "0.1.0"
edition = "2024"

[dependencies]



[profile.release]
debug = true

[features]
    default = ["semantics"]
    codegen = ["semantics"]
    semantics = ["parse"]
    parse = ["lex"]
    lex = []

